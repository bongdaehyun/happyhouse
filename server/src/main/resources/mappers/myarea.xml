<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.repo.AreaApartRepo">

	<select id="selectarea" resultType="myArea">
		SELECT * FROM areainfo where dongcode= #{dongcode}
	</select>
	<select id="getAreacode" resultType="String">
		select code from baseaddress where city= #{city} and gugun= #{gugun} and dong= #{dong}
	</select>
	<update id="updateArea">
		update user set code = #{code} where userid= #{userid}
	</update>
	<select id="getdong" resultType="String">
		select dong from dongcode where dongcode=#{code};
	</select>
	<select id="getCode" resultType="DongcodeDto">
		select city,gugun,dong from baseaddress where code=#{code}
	</select>
</mapper>